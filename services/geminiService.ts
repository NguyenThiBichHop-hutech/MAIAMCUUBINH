import { DonorInfo } from "../types";

// Tính năng AI đã được gỡ bỏ theo yêu cầu.
// File này được giữ lại (trống) để tránh lỗi import nếu có reference sót.

export const generateThankYouLetter = async (donor: DonorInfo): Promise<string> => {
  return "Tính năng soạn thư tự động đã bị vô hiệu hóa.";
};